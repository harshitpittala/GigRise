# scripts/

One-off/maintenance scripts — seed data, OpenAPI→TypeScript codegen, migration helpers (`PROJECT_SETUP.md` §2).

Empty at Phase 0.1 (Repository & Monorepo Foundation). The first script this directory will hold is the OpenAPI→TS codegen step, once `apps/api` exposes real endpoints and `packages/types` has a contract to generate against.
