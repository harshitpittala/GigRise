import type { NextConfig } from "next";

// Framework-default config at Phase 0.1 — no feature-specific configuration
// added until a feature actually needs it.
const nextConfig: NextConfig = {};

export default nextConfig;
