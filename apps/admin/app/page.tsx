export default function Home() {
  // Placeholder scaffold page only — Phase 0.1 (Repository & Monorepo
  // Foundation) explicitly excludes UI pages and business logic. The real
  // CRM shell is built in IMPLEMENTATION_PLAN.md Phase 3.
  return <p>GigRise — apps/admin scaffold. No product UI yet.</p>;
}
