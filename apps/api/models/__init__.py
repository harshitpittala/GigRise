"""SQLAlchemy ORM models (PROJECT_SETUP.md §4) — one module per
DATABASE_ARCHITECTURE.md cluster (identity.py, financial.py, ...). Empty at
Phase 0.2: only `core.database.Base` (the declarative base every future
model inherits from) is configured this phase — "NO database models" is
explicit in this phase's scope."""
