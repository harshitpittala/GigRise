"""Query layer (PROJECT_SETUP.md §4) — every ownership-scoped query
(AUTHENTICATION.md §14 layer 3) lives here, never inline in a router.
Empty at Phase 0.2 — "NO repositories" is explicit in this phase's scope."""
