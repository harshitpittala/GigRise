"""Async job handlers (PROJECT_SETUP.md §4) — media transcoding callbacks,
notification fan-out, analytics rollups, scheduled sweeps. Empty at Phase
0.2, since none of the features that would need a worker exist yet."""
