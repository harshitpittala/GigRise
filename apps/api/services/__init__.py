"""Business logic (PROJECT_SETUP.md §4), organized as internal packages
named after ARCHITECTURE.md §11's service list (services/messaging/,
services/payments/, services/media/, services/notification/,
services/search/, services/ai/) once real feature work begins. Empty at
Phase 0.2 — "NO services, NO business logic" is explicit in this phase's
scope; the named sub-packages are not pre-created either, since structure
should follow real content, not precede it."""
