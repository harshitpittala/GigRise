export default function Home() {
  // Placeholder scaffold page only — Phase 0.1 (Repository & Monorepo
  // Foundation) explicitly excludes UI pages and business logic. Real
  // pages are built starting with IMPLEMENTATION_PLAN.md's later phases.
  return <p>GigRise — apps/web scaffold. No product UI yet.</p>;
}
