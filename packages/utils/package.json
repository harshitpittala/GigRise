{
  "name": "@gigrise/utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure TypeScript helper functions shared across apps (PROJECT_SETUP.md §2). Empty placeholder at Phase 0 — no business logic exists yet to extract a shared helper from.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@gigrise/config": "workspace:*",
    "typescript": "^7.0.2"
  }
}
