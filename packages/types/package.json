{
  "name": "@gigrise/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared TypeScript types, generated from the OpenAPI spec once it exists (API_SPECIFICATION.md §24). Empty placeholder at Phase 0 — no API contract exists yet to generate from.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@gigrise/config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
