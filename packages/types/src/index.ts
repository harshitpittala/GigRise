// Intentionally empty at Phase 0.1 (Repository & Monorepo Foundation).
//
// This package will hold TypeScript types generated from the OpenAPI spec
// once apps/api exposes real endpoints (API_SPECIFICATION.md §24). No types
// are hand-authored here — inventing shapes ahead of a real API contract
// would risk drifting from API_SPECIFICATION.md, which this project's
// MASTER_DEVELOPMENT_GUIDE.md §30 explicitly forbids ("never invent an API
// endpoint" extends to never inventing the types that would back one).
export {};
